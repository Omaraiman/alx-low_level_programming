#include "main.h"
#include "_putcahr.c"

/**
*main - return valu
*
*Desctiption: A C program that return 1 or 0 in fuction called isupper
*
*Return: 0 , 1
*/

int isupper(void)
{
int count;
int d;
for (count = 'A'; count <= 'Z'; count++)
{
if (d != count)
{
return (1);
}
return (0);
}
}
