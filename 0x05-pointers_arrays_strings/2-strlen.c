#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: the length of array = i
 */

int _strlen(char *s)
{
	int s[] = "My first strlen!";

	int i;

	for (i = 0; s[i] != '\0'; i++)

	return (i);
}
